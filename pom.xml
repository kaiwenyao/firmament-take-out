<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <artifactId>spring-boot-starter-parent</artifactId>
    <groupId>org.springframework.boot</groupId>
    <version>3.5.9</version>
  </parent>
  <groupId>dev.kaiwen</groupId>
  <artifactId>firmament-take-out</artifactId>
  <packaging>pom</packaging>
  <version>1.0-SNAPSHOT</version>
  <modules>
    <module>firmament-common</module>
    <module>firmament-pojo</module>
    <module>firmament-server</module>
  </modules>
  <properties>
    <lombok>1.18.42</lombok>
    <fastjson2>2.0.60</fastjson2>
    <commons.lang>20030203.000129</commons.lang>
    <org.mapstruct.version>1.6.3</org.mapstruct.version>
    <druid>1.2.23</druid>
    <aliyun.sdk.oss>3.18.3</aliyun.sdk.oss>
    <jjwt>0.12.5</jjwt>
    <poi>5.4.0</poi>
    <springdoc-openapi>2.8.14</springdoc-openapi>
    <mybatis-plus>3.5.15</mybatis-plus>
    <jackson-databind>2.17.0</jackson-databind>
    <mysql-connector-j>8.0.33</mysql-connector-j>
    <wechatpay-apache-httpclient>0.4.8</wechatpay-apache-httpclient>
    <!-- 测试依赖版本 -->
    <testcontainers.version>1.21.3</testcontainers.version>
    <jacoco.exec.file>jacoco.exec</jacoco.exec.file>
  </properties>
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>com.baomidou</groupId>
        <artifactId>mybatis-plus-bom</artifactId>
        <version>${mybatis-plus}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.mapstruct</groupId>
        <artifactId>mapstruct</artifactId>
        <version>${org.mapstruct.version}</version>
      </dependency>
      <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <version>${lombok}</version>
      </dependency>

      <dependency>
        <groupId>com.alibaba.fastjson2</groupId>
        <artifactId>fastjson2</artifactId>
        <version>${fastjson2}</version>
      </dependency>

      <dependency>
        <groupId>commons-lang</groupId>
        <artifactId>commons-lang</artifactId>
        <version>${commons.lang}</version>
      </dependency>

      <dependency>
        <groupId>com.alibaba</groupId>
        <artifactId>druid-spring-boot-starter</artifactId>
        <version>${druid}</version>
      </dependency>


      <!-- SpringDoc OpenAPI (官方 Swagger) -->
      <dependency>
        <groupId>org.springdoc</groupId>
        <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        <version>${springdoc-openapi}</version>
      </dependency>

      <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt</artifactId>
        <version>${jjwt}</version>
      </dependency>

      <dependency>
        <groupId>com.aliyun.oss</groupId>
        <artifactId>aliyun-sdk-oss</artifactId>
        <version>${aliyun.sdk.oss}</version>
      </dependency>

      <!-- Jackson Databind -->
      <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-databind</artifactId>
        <version>${jackson-databind}</version>
      </dependency>

      <!-- MySQL Connector -->
      <dependency>
        <groupId>com.mysql</groupId>
        <artifactId>mysql-connector-j</artifactId>
        <version>${mysql-connector-j}</version>
      </dependency>

      <!-- poi -->
      <dependency>
        <groupId>org.apache.poi</groupId>
        <artifactId>poi</artifactId>
        <version>${poi}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.poi</groupId>
        <artifactId>poi-ooxml</artifactId>
        <version>${poi}</version>
      </dependency>
      <!--微信支付-->
      <dependency>
        <groupId>com.github.wechatpay-apiv3</groupId>
        <artifactId>wechatpay-apache-httpclient</artifactId>
        <version>${wechatpay-apache-httpclient}</version>
      </dependency>

      <!-- Testcontainers BOM（集成测试用：MySQL + Redis 容器） -->
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers-bom</artifactId>
        <version>${testcontainers.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>
  <build>
    <plugins>
      <!-- 静态代码分析：SpotBugs（字节码缺陷） + Checkstyle（源码约定）。
           两者都绑定进 Maven 生命周期——本地 mvn verify 与 Jenkins 流水线共用同一套门禁。
           注意两个插件都按相对路径找根目录下的配置文件，需从仓库根目录执行 mvn。 -->
      <plugin>
        <groupId>com.github.spotbugs</groupId>
        <artifactId>spotbugs-maven-plugin</artifactId>
        <version>4.9.8.0</version>
        <configuration>
          <excludeFilterFile>spotbugs-exclude.xml</excludeFilterFile>
          <!-- Max 力度全量分析；Medium 阈值即 CI 门禁线。
               不开 xmlOutput/htmlOutput：XML 报告默认就会写 target/spotbugsXml.xml
              （check 目标与 Jenkins 归档都读它）；显式开启这两个开关反而会触发插件
               4.9.8.0 的已知问题（github issue #965）：XML 汇总元数据损坏
              （total_classes=0 等），归档报告会误导排障，故保持默认关闭。 -->
          <effort>Max</effort>
          <threshold>Medium</threshold>
        </configuration>
        <executions>
          <!-- 绑定到 verify：需要先编译出 class 才能分析。
              Jenkins「静态代码分析」阶段显式调用同一目标，两条路径规则一致。 -->
          <execution>
            <id>spotbugs-check</id>
            <phase>verify</phase>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-checkstyle-plugin</artifactId>
        <version>3.6.0</version>
        <configuration>
          <configLocation>checkstyle.xml</configLocation>
          <!-- 插件默认捆绑 checkstyle 9.3（只认 Java 17 语法），显式升级到
               12.3.1 以支持 record pattern / switch pattern 等新语法。
               12.x 字节码仍是 Java 17，与本模块（含 Jenkins temurin-17 镜像）兼容。 -->
          <includeTestSourceDirectory>false</includeTestSourceDirectory>
        </configuration>
        <dependencies>
          <dependency>
            <groupId>com.puppycrawl.tools</groupId>
            <artifactId>checkstyle</artifactId>
            <version>12.3.1</version>
          </dependency>
        </dependencies>
        <executions>
          <!-- 绑定到 validate：任何生命周期构建（test/package/verify）最先跑风格检查，
              快速失败，不用等编译和测试才发现格式问题 -->
          <execution>
            <id>checkstyle-check</id>
            <phase>validate</phase>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>0.8.12</version>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
            <configuration>
              <!-- 排除 jsqlparser：其 JavaCC 生成的词法分析器方法超出字节码大小上限，
                   会导致 JaCoCo 抛出 MethodTooLargeException 噪声。覆盖率仅关注业务代码。 -->
              <excludes>
                <exclude>net/sf/jsqlparser/**</exclude>
              </excludes>
              <destFile>${project.build.directory}/${jacoco.exec.file}</destFile>
            </configuration>
          </execution>
          <execution>
            <id>merge-coverage</id>
            <phase>none</phase>
            <goals>
              <goal>merge</goal>
            </goals>
            <configuration>
              <destFile>${project.build.directory}/jacoco.exec</destFile>
              <fileSets>
                <fileSet>
                  <directory>${project.build.directory}</directory>
                  <includes>
                    <include>jacoco-ut.exec</include>
                    <include>jacoco-it.exec</include>
                  </includes>
                </fileSet>
              </fileSets>
            </configuration>
          </execution>
          <execution>
            <id>report-merged</id>
            <phase>none</phase>
            <goals>
              <goal>report</goal>
            </goals>
            <configuration>
              <dataFile>${project.build.directory}/jacoco.exec</dataFile>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
